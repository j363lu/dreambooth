# Bottle DreamBooth Results

The images in this folder were generated from a model fine-tuned on
`data/our_data/bottle`.

`sks` is the special token used to refer to the fine-tuned bottle subject. Each
image filename is the prompt that was used to generate that image.
