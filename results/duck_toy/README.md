# Duck Toy DreamBooth Results

The images in this folder were generated from a model fine-tuned on
`data/dreambooth_original/duck_toy`.

`sks` is the special token used to refer to the fine-tuned duck toy subject.
Each image filename is the prompt that was used to generate that image.
